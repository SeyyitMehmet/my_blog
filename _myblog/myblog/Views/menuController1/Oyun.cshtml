@model IEnumerable<Entitylayer.Concrate.Menu>

@foreach (var item in Model)
{
    <div class="cardjj mb-3 w-100 bg-dark text-light mt-1" style="">
        <div class="g-0">
            <div class=" d-flex justify-content-center ">
             @*   <img src="@item." style="height:contain;width:40%" class=" p-2" />*@
            </div>
            <div class="">
                <div class="card-body">
                  @*  <h5 class="card-title d-flex justify-content-center text-decoration-underline">@item.BlogTitle</h5>*@
                    <p class="card-text">
                        @Html.Raw(item.Content)
                    </p>
                    <p class="card-text"><small class="text-body-secondary">Last updated 3 mins ago</small></p>
                </div>
            </div>
        </div>
    </div>

}
